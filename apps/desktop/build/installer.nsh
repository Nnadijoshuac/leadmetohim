; NSIS custom installer script for LeadMeToHim
; Runs after the main installer logic

!macro customInit
  ; Check for existing installation and clean up if needed
!macroend

!macro customInstall
  ; Register the app to run at startup (optional, mirrors app setting)
!macroend

!macro customUnInstall
  ; Clean up user data on uninstall (optional - prompt user)
!macroend
